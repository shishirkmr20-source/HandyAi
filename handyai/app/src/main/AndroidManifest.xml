<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- For saving generated images to gallery on Android 8/9 (API 26-28).
         Android 10+ uses scoped storage and doesn't need this. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- LiteRT-LM (vision models like FastVLM) declares minSdk 31 in its
         AndroidManifest. Our app supports Android 8+ (minSdk 26). This
         override forces the manifest merger to accept the library on
         older devices. We guard FastVLM activation at runtime with a
         Build.VERSION.SDK_INT >= 31 check in ModelSettingsViewModel,
         so users on Android 8-11 will see the FastVLM catalog entry
         but activation will fail with a friendly "requires Android 12+"
         message instead of crashing the app. -->
    <uses-sdk tools:overrideLibrary="com.google.ai.edge.litertlm" />

    <application
        android:name=".HandyAiApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HandyAi"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.HandyAi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
